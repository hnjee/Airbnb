<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper	namespace="com.airbnb.s1.booking.BookingDAO">

	<insert id="booking" parameterType="BookingVO">
		insert into booking values(#{bookingNum},#{placeNum},#{memberNum}, 1,#{checkInDate},#{checkOutDate},#{payType},#{payInfo},#{payTotal},sysdate)
	</insert>

</mapper>